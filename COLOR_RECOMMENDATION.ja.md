# 🎨 色のトレンドと提案ロジック — Personal Color AI

**🌐 言語 / Language / Ngôn ngữ:** [Tiếng Việt](./COLOR_RECOMMENDATION.md) · [English](./COLOR_RECOMMENDATION.en.md) · **日本語**

本ドキュメントは、**システムがどのように色を提案するか** を説明します。AIモデルによる「パーソナルシーズン」の判定から、**シーン**（時間帯 + イベント）に基づく提案レイヤー、**人種** / **カスタムカラー** による個別最適化までを扱います。

---

## 1. 2つの独立したレイヤー：分類 と 提案

設計の核心：

| レイヤー | 入力 | 出力 | シーンで変わる？ |
|---|---|---|---|
| **シーズン分類（AIモデル）** | 肌・髪・目・光（50特徴量） | 16サブシーズンのいずれか + 信頼度 | ❌ 変わらない。身体の固定的属性。 |
| **色の提案（ロジック）** | 判定済みシーズン + シーン | パレットを **並べ替え + ★印** | ✅ 変わる。時間帯/イベントは *優先順位* のみ変更。 |

> **原則：** 時間帯やイベントの種類は、パーソナルシーズンを **変えません**。「オータム・ディープ」の人は、朝でも夜でも、パーティーでも自宅でも「オータム・ディープ」のままです。これらは **そのシーズンのパレットの中でどの色** がその場に最も合うかを決めるだけです。したがってこれらの要素は **提案レイヤー** にあり、モデルの入力ではありません。

---

## 2. パーソナルシーズンの判定方法

モデル（RandomForest、50特徴量）はパーソナルカラー理論の4軸から学習します：

- **Warmth（暖 ↔ 寒）：** 肌の `a*`（赤）と `b*`（黄）+ 髪のアンダートーンに基づく。
  暖 → スプリング / オータム · 寒 → サマー / ウィンター。
- **Depth（明 ↔ 深）：** `depth_score = 0.7·(100−face_L) + 0.3·(100−hair_L)`。
  **肌が支配的（0.7）**、髪は補助（0.3）— 肌が明るく髪が黒い人が「deep」シーズンと誤判定されないように。
- **Clarity（澄 ↔ くすみ）：** 全体のコントラスト + 目の澄み具合。
  高 → Clear/Bright · 低 → Soft/Muted。
- **ITA（Individual Typology Angle）：** 科学的な肌フォトタイプ指標。

**人種ベースライン**（アジア / 白人 / アフリカ）は残差 `face_L_res = face_L − baseline_L` の計算に使い、絶対値ではなく「その人自身の人種的基準に対して肌が明るい/暗い」を評価します。

---

## 3. 色の特性 — 色の「採点」方法

各 HEX コードは LAB に変換され、3つの特性に還元されます（`hexToColorTraits`）：

| 特性 | 意味 | 式 |
|---|---|---|
| **L**（明度） | 明 ↔ 暗 | LAB の `L`（0–100） |
| **chroma** | 鮮やかさ/彩度 | `√(a² + b²)` |
| **warmth** | 暖 ↔ 寒 | `b + 0.3·a`（b：黄青軸、a：暖かい赤を少し加算） |

基本の採点関数（0…1 を返す）：

- `prefHigh(v, min, max)` — 大きいほど良い（例：明るい色を優先）。
- `prefLow(v, min, max)` — 小さいほど良い（例：深い色を優先）。
- `near(v, target, tol)` — 目標値に近いほど良い（例：ニュートラル）。

---

## 4. 時間帯による優先ルール

| 時間帯 | 考え方 | スコア（重み付き合計） |
|---|---|---|
| **朝** | 強い日光 → 明るく爽やか・中彩度 | `0.5·prefHigh(L,40,90) + 0.3·near(chroma,35) + 0.2·prefHigh(warmth)` |
| **昼** | ニュートラル・やや暖かい | `0.4·near(L,60) + 0.3·near(chroma,40) + 0.3·prefHigh(warmth)` |
| **夜** | 人工照明で淡色が飛ぶ → 深く鮮やか・高コントラスト | `0.5·prefLow(L) + 0.4·prefHigh(chroma,20,70) + 0.1·prefLow(warmth)` |

---

## 5. イベントによる優先ルール

| イベント | 考え方 | 主な優先 |
|---|---|---|
| **パーティー** | 上品で印象的 | 深い + 鮮やか（`prefLow(L)` + `prefHigh(chroma)`） |
| **誕生日** | 明るく華やか | 明るい + 鮮やか + やや暖かい |
| **デート** | 柔らかく肌映え | 中間トーン・やや暖かい（`near(L,62)` + `prefHigh(warmth)`） |
| **オフィス** | 控えめでプロフェッショナル | ニュートラル + **低彩度**（`prefLow(chroma)`） |
| **屋内** | 暖色系の人工照明 | 暖色トーン・中明度 |
| **屋外** | 日光 | 明るく爽やか・やや涼しい（`prefHigh(L)` + `prefLow(warmth)`） |
| **普段着** | 快適・バランス | `near(L,60)` + `near(chroma,35)` |

---

## 6. スコアの統合と ★印

```
score(色) =
    time = "any"  かつ event = "any"  → 0.5（ニュートラル、並べ替えなし）
    time のみ                         → 時間帯スコア
    event のみ                        → イベントスコア
    両方                              → 0.5·time + 0.5·event
```

その後（`applyContextPreferences`）：

1. スコアで **降順ソート**（安定 — 同点は元の順序を維持）。
2. 上位 **約40%** の色に **★印**（そのシーンで優先）。
3. **すべてのセクション** に適用：パレット、カラーグループ、メイク（口紅/チーク/アイ）、髪色、服（トップス/ボトムス）。
   **「避けるべき色」** グループは対象外 — シーンに関係なく常に避けます。

`time = any` かつ `event = any` の場合 → 元の順序で表示し、★印も説明バナーも **なし**。

---

## 7. さらなる個別最適化

### 人種（Ethnicity）
ユーザーがアジア / 白人 / アフリカを選択 → 写真からの推定ではなく **実際の LAB ベースライン**（学習データセットに一致）を使用。これは **モデルを再実行** し（`face_*_res` 特徴量が変化）、特に肌が明るく髪が黒い人の精度を向上させます。

### カスタムカラー（color override）
ユーザーはカラーピッカーで **肌 / 目 / 髪 / 首の色** を再指定できます。その場合：

```
選んだ hex → LAB → 特徴量ベクトルに置換 → AIモデルを再実行
          → 新しいシーズン（変化する場合）→ 選んだ色に合わせてすべての提案が更新
```

**元に戻す** ボタンで、写真から測定した色に戻せます。

---

## 8. 提案品質に関する注意

- 上記ルールは **色理論に基づくヒューリスティック** であり、ユーザーのフィードバックから学習したものではありません。*妥当に並べ替える* ことが目的で、絶対的な正解ではありません。
- 調整方法：`web/src/app.js`（6b節）の `TIME_PROFILES` / `EVENT_PROFILES` を編集。各プロファイルは `{L, chroma, warmth}` を受け取り 0…1 を返す関数です。
- ★の割合（デフォルト40%）は `applyContextPreferences` の `highlightRatio` パラメータで設定します。
- 目標しきい値（例：`near(L, 60)`）は、濃い/淡い好みに合わせて調整できます。

---

## 9. 提案フロー図

```
写真 ──► LAB抽出（肌/髪/目）──► [人種ベースライン] ──► AIモデル ──► パーソナルシーズン
                                                                              │
                                       (ユーザーの color override) ────────────┤
                                                                              ▼
                          シーズンの基本パレット  ──►  applyContextPreferences(時間帯, イベント)
                                                                              ▼
                                 最終提案：並べ替え + ★印 + 説明バナー
```
